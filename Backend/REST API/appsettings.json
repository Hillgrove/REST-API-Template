{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "LocalDb": "Server=(localdb)\\MSSQLLocalDB;Database=ChairDb;Trusted_Connection=True;",
    // TODO: Replace connection string or use secrets
    "RemoteDb": "Server=your_remote_server;Database=ChairDb;User Id=your_username;Password=your_password;"
  },

  "RepositorySettings": {
    "UseDatabase": "false", // or "false" to use the list repository
    "DatabaseType": "LocalDb" // or "RemoteDb" to use the remote database"
  }
}
